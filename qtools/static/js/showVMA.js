// Generated by CoffeeScript 1.7.1

/*
vma
 */
 VMA_state = 0;

function showVMA()
{
  var i, offset, point_height, point_width, vctx, vma_point, vol, x, x_step, _i, _len;

  x_step = 10;

  point_width = 2;

  point_height = 2;

  offset = 10;

  x = 0;

  vol = document.getElementById('vol');
  btn = document.getElementById('vmabtn');
  vctx = vol.getContext('2d');

  if (VMA_state == 0 ) {
      //vctx.translate(0.5, 0.5);
    for (i = _i = 0, _len = vma_data.length; _i < _len; i = ++_i) {
      vma_point = vma_data[i];
      vctx.rect(x + (i + offset) * x_step, vma_point, point_width, point_height);

    }
    //change button color
    btn.style.backgroundColor="#66CCFF";
  vctx.stroke();
  VMA_state=1;
  }
  else {
    for (i = _i = 0, _len = vma_data.length; _i < _len; i = ++_i) {
      vma_point = vma_data[i];
      vctx.clearRect(x + (i + offset) * x_step-2, vma_point-2, point_width+6, point_height+6);
    }
    btn.style.backgroundColor="#5768b3";
    VMA_state=0;
  }
};
